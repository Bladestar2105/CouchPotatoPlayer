import { requireNativeComponent } from 'react-native';

/**
 * KSPlayerNativeView — low-level native component.
 * Use the KSPlayerView React component from components/KSPlayerView.tsx instead.
 */
export const KSPlayerNativeView = requireNativeComponent('KSPlayerView');