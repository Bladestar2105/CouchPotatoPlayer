import { View } from 'react-native';

// Web stub — requireNativeComponent is not available on web.
// KSPlayerView.tsx already gates on Platform.OS === 'ios', so this component
// is never actually rendered on web.
export const KSPlayerNativeView = View;
