import { View } from 'react-native';

// Web stub — requireNativeComponent is not available on web.
// SwiftTSPlayer / AppleAVPlayer are only rendered on iOS; on web the
// VideoPlayer routes to the 'web' case and never reaches this component.
export const SwiftTSPlayerView = View;
export const SwiftTSPlayerProxyModule = null;
