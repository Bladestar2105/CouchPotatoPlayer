commit a56329178e254e84499815666389c9c2eada6080
Author: BigLisBigger <BigLisBigger@users.noreply.github.com>
Date:   Fri Mar 13 10:51:57 2026 +0000

    perf: optimize HLS.js buffering for smooth IPTV playback

    - Disabled lowLatencyMode (was causing constant rebuffering)
    - Increased buffer from 30s/60s to 60s/120s ahead
    - Added 60MB max buffer size
    - Set liveSyncDurationCount=3, liveMaxLatencyDurationCount=6
    - Increased retry attempts to 10 with 1s delay
    - Added 30s fragment loading timeout
    - Improved error recovery with attempt tracking and auto-reset
    - Non-fatal errors no longer spam console logs

diff --git a/ios/Podfile b/ios/Podfile
new file mode 100644
index 0000000..d7f0d66
--- /dev/null
+++ b/ios/Podfile
@@ -0,0 +1,55 @@
+# Resolve react_native_pods.rb with node to allow for hoisting
+require Pod::Executable.execute_command('node', ['-p',
+  'require.resolve(
+    "react-native/scripts/react_native_pods.rb",
+    {paths: [process.argv[1]]},
+  )', __dir__]).strip
+
+prepare_react_native_project!
+
+linkage = ENV['USE_FRAMEWORKS']
+if linkage != nil
+  Pod::UI.puts "Configuring Pod with #{linkage}ally linked Frameworks".green
+  use_frameworks! :linkage => linkage.to_sym
+end
+
+source 'https://github.com/CocoaPods/Specs.git'
+
+def shared_pods(config)
+  pod 'KSPlayer', :git => 'https://github.com/kingslay/KSPlayer.git'
+  pod 'DisplayCriteria', :git => 'https://github.com/kingslay/KSPlayer.git', :modular_headers => true
+  pod 'FFmpegKit', :git => 'https://github.com/kingslay/FFmpegKit.git', :tag => '6.1.3', :modular_headers => true
+  pod 'Libass', :git => 'https://github.com/kingslay/FFmpegKit.git', :tag => '6.1.3'
+  pod 'Libmpv', :git => 'https://github.com/kingslay/FFmpegKit.git', :tag => '6.1.3'
+  pod 'OpenSSL', :git => 'https://github.com/kingslay/FFmpegKit.git', :tag => '6.1.3'
+
+  use_react_native!(
+    :path => config[:reactNativePath],
+    # An absolute path to your application root.
+    :app_path => "#{Pod::Config.instance.installation_root}/.."
+  )
+end
+
+target 'CouchPotatoPlayer' do
+  platform :tvos, min_ios_version_supported
+  config = use_native_modules!
+  shared_pods(config)
+end
+
+target 'CouchPotatoPlayer-iOS' do
+  platform :ios, min_ios_version_supported
+  config = use_native_modules!
+  shared_pods(config)
+end
+
+post_install do |installer|
+  # Use the config from the first target (or fetch it globally if needed)
+  # react_native_post_install needs the React Native path.
+  # We can just construct it or use the env.
+  react_native_post_install(
+    installer,
+    "../node_modules/react-native",
+    :mac_catalyst_enabled => false,
+    # :ccache_enabled => true
+  )
+end
