If `canImport(Flutter)` is false on tvOS, why would `url_launcher_ios` compile?
Because `url_launcher_ios` was patched to `#if os(iOS) || os(tvOS) \n import Flutter`. It bypasses `canImport`.
BUT WAIT! If `url_launcher_ios` bypassed `canImport` and forced `import Flutter`, did it SUCCESS or did it FAIL?!
If it succeeded, then `import Flutter` works.
If it failed, then `url_launcher_ios` WOULD HAVE FAILED!
But it succeeded!
If `url_launcher_ios` succeeded, `Flutter` module MUST exist!
If `Flutter` module exists, WHY DID `path_provider_foundation` FAIL with `no such module 'Flutter'`?!
Wait... could `path_provider_foundation` be missing a `-F` path to `Flutter` in SOME other build configuration, or did it fail for a different file?
No, the log literally says:
`/Users/runner/.pub-cache/hosted/pub.dev/path_provider_foundation-2.5.1/darwin/path_provider_foundation/Sources/path_provider_foundation/messages.g.swift:10:10: error: no such module 'Flutter'`
There is NO OTHER interpretation. The compiler CANNOT FIND `Flutter` when compiling `path_provider_foundation`.

Let's look at `url_launcher_ios` again.
Is it possible that `url_launcher_ios` DID NOT EVEN COMPILE?
No, we saw `CompileSwift normal arm64`.

What if the custom tvOS engine `Flutter.framework` HAS A TYPO or is missing something that `path_provider_foundation` specifically needs?
No, `no such module 'Flutter'` means it can't find the MODULEMAP or the framework itself.

What if the framework search path is evaluated incorrectly because of spaces or quotes?
`-F /Users/runner/work/CouchPotatoPlayer/CouchPotatoPlayer/ios/Flutter`
It is perfectly formatted.

Let's check the memory:
"When patching Pigeon-generated Swift files for tvOS compatibility in `tvos/Podfile`, ensure the string replacement logic accounts for formatting variations... "
I already did this with the regex!
And I proved the regex matches and patches the file!
Wait! Does `path_provider_foundation` have ANOTHER file that needs patching?
What if `PathProviderPlugin.swift` ALSO has `import Flutter`?!
Yes, it does:
```swift
import Foundation

#if os(iOS) || os(tvOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#endif
```
It was patched too (in `test_crlf6.rb`).
Why did it fail?
