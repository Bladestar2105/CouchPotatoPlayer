events {}
http {
    error_log /app/error.log debug;
    server {
        listen 80;

        # Okay, so the upstream returns 502 for that URL NO MATTER WHAT!
        # The user reported: "GET /proxy/https://stream.team-ei.de/movie/yoshisan/Ss21051988/1.mp4 HTTP/1.1" 502
        # BUT the NGINX configuration HAS A FUNDAMENTAL BUG in the REGEX block!
        # It's broken for ALL URLs because `proxy_pass $target_url;` without URI rewrite causes NGINX to pass the original `$request_uri`!
        # Which means NO request to ANY server through the REGEX block works!
        # Let's prove it!
    }
}
