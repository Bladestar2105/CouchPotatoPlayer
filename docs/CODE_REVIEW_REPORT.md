<h1>Code Review &amp; Optimierungsanalyse: CouchPotatoPlayer (iOS/tvOS)</h1><p><strong>Analyse-Datum:</strong> April 2025<br><strong>Repository:</strong> Bladestar2105/CouchPotatoPlayer<br><strong>Plattformen:</strong> iOS, tvOS (Apple TV), Android, Android TV, Web<br><strong>Technologie-Stack:</strong> React Native + Expo, Swift (Native Modules), KSPlayer/FFmpegKit</p><hr><h2>Executive Summary</h2><p>CouchPotatoPlayer ist eine gut strukturierte IPTV-Player-Anwendung mit einer soliden Architektur für Multi-Platform-Support. Die Codebasis zeigt professionelle Entwurfsmuster mit klarer Trennung zwischen React Native UI-Logik und nativen Player-Implementierungen. Dennoch bestehen mehrere Optimierungsmöglichkeiten, insbesondere im Bereich Speicherverwaltung, tvOS-Focus-Engine-Integration und Performance-Tuning für große Kanallisten.</p><p><strong>Gesamtbewertung:</strong> ⭐⭐⭐⭐ (4/5) - Gute Basis mit Verbesserungspotenzial</p><hr><h2>Detaillierte Optimierungsanalyse</h2><hr><h3>1. PERFORMANCE-OPTIMIERUNGEN</h3><hr><h4>1.1 Speicherverwaltung in Native Playern</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Performance</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td>In <code>KSPlayerView.swift</code> (Zeile 79-91) wird <code>deinit</code> asynchron auf dem Main-Thread ausgeführt. Der Timer-basierte Retry-Mechanismus in <code>SwiftTSPlayerView.swift</code> (Zeile 78-85) nutzt <code>Timer.scheduledTimer</code>, das starke Referenzen halten kann.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Verwendung von <code>[weak self]</code> in allen Timer-Closures und Implementierung einer expliziten <code>cleanup()</code>-Methode, die vor <code>deinit</code> aufgerufen wird. Für KSPlayer sollte der Teardown synchron erfolgen, wenn möglich, oder mit <code>DispatchWorkItem</code> abgebrochen werden können.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Asynchrone Cleanup-Operationen in <code>deinit</code> können zu Race Conditions führen, wenn die Instanz bereits freigegeben wurde, aber der Block noch ausgeführt wird. Timer halten starke Referenzen auf <code>self</code>, was zu Memory Leaks führt, wenn sie nicht explizit invalidiert werden.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Hoch</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>iOS/tvOS (Beide)</td> </tr> </tbody></table><p><strong>Code-Beispiel (KSPlayerView.swift):</strong></p><pre><code class="language-swift">// AKTUELL
deinit {
    let layer = playerLayer
    playerLayer = nil
    if let layer {
        DispatchQueue.main.async {
            layer.delegate = nil
            layer.player.view?.removeFromSuperview()
            layer.stop()
        }
    }
}

// OPTIMIERT
private var cleanupWorkItem: DispatchWorkItem?

deinit {
    cleanupWorkItem?.cancel()
    cleanupWorkItem = nil
    // Synchroner Teardown wo möglich
    playerLayer?.delegate = nil
    playerLayer?.stop()
    playerLayer = nil
}
</code></pre><hr><h4>1.2 React Native Re-Render Optimierung</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Performance</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td>In <code>ChannelList.tsx</code> sind <code>CategoryItem</code> und <code>ChannelRow</code> bereits mit <code>React.memo</code> optimiert. Die <code>renderChannelItem</code>-Callback (Zeile 340-366) wird jedoch bei jedem Render neu erstellt, da sie von mehreren Werten abhängt, die sich häufig ändern.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Aufteilung der <code>renderChannelItem</code>-Funktion in stabilere Teilfunktionen mit <code>useCallback</code> und Verwendung von <code>useMemo</code> für berechnete Werte wie <code>channelIndexOffset</code>. Implementierung eines Custom-Hooks für EPG-Daten-Lookup.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Die aktuelle Implementierung führt zu unnötigen Re-Renders aller sichtbaren Kanäle, wenn sich <code>focusedChannelId</code> oder <code>currentStream?.id</code> ändert. Bei großen Kanallisten (500+ Kanäle) kann dies zu spürbarer Verzögerung führen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Hoch</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>Beide</td> </tr> </tbody></table><p><strong>Code-Beispiel:</strong></p><pre><code class="language-typescript">// Neuer Custom Hook für EPG-Lookup
const useChannelEpgInfo = (channel: Channel, epg: Record&lt;string, any[]&gt;) =&gt; {
  return useMemo(() =&gt; {
    const key = channel.epgChannelId || channel.tvgId || channel.id;
    const programs = epg[key] || [];
    if (programs.length === 0) return { currentProgram: null, progressPercent: 0 };
    
    const now = Date.now();
    const idx = findCurrentProgramIndex(programs, now);
    if (idx === -1) return { currentProgram: null, progressPercent: 0 };
    
    const prog = programs[idx];
    const totalDuration = prog.end - prog.start;
    const elapsed = now - prog.start;
    return { 
      currentProgram: prog, 
      progressPercent: Math.min(100, Math.max(0, (elapsed / totalDuration) * 100))
    };
  }, [channel.epgChannelId, channel.tvgId, channel.id, epg]);
};
</code></pre><hr><h4>1.3 Bild-Caching und Prefetching</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Performance</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>imageCache.ts</code> implementiert ein einfaches <code>Set&lt;string&gt;</code> für bereits geladene URLs mit einem Limit von 500 Einträgen. Das Prefetching erfolgt synchron beim Scrollen, was zu Frame-Drops führen kann.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung eines LRU-Cache mit Größenbeschränkung (z.B. 50MB) statt reiner URL-Anzahl. Verwendung von <code>InteractionManager.runAfterInteractions</code> für Prefetching, um Scroll-Performance nicht zu beeinträchtigen.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Das aktuelle System speichert nur URLs, nicht die eigentlichen Bilddaten. Bei Kanallogos, die mehrfach angezeigt werden (Favorites, kürzlich gesehen, Kanalliste), führt dies zu wiederholten Netzwerkrequests.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>Beide</td> </tr> </tbody></table><hr><h4>1.4 FlatList Virtualization Tuning</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Performance</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>ChannelList.tsx</code> verwendet <code>listPerfConfig</code> mit <code>windowSize: 9</code> für TV und <code>removeClippedSubviews: false</code>. Die <code>getItemLayout</code> ist korrekt implementiert für feste Zeilenhöhen.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Erhöhung von <code>windowSize</code> auf <code>11</code> für tvOS (größere Bildschirme) und Implementierung von <code>CellRendererComponent</code> mit <code>React.memo</code> für konsistente Render-Performance. Aktivierung von <code>removeClippedSubviews: true</code> für iOS nicht-TV Geräte.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Auf Apple TV mit großen Bildschirmen werden mehr Kanäle gleichzeitig angezeigt. <code>windowSize: 9</code> kann zu "leeren" Zellen beim schnellen Scrollen führen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><hr><h3>2. PLATTFORM-SPEZIFISCHE FEATURES</h3><hr><h4>2.1 tvOS Focus Engine Verbesserung</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Plattform-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>TVFocusGuideView.tsx</code> ist eine korrekte Abstraktion über <code>TVFocusGuideView</code>, aber die Verwendung in <code>HomeTVLayout.tsx</code> und <code>PlayerScreen.tsx</code> ist nicht optimal. Die <code>destinations</code>-Prop wird in <code>PlayerScreen.tsx</code> (Zeile 756) mit Refs gesetzt, die erst nach dem Mount verfügbar sind.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung einer <code>useFocusGuide</code>-Hook, die automatisch die nächsten fokussierbaren Elemente ermittelt und an <code>destinations</code> übergibt. Verwendung von <code>hasTVPreferredFocus</code> nur beim initialen Mount, nicht bei jedem State-Update.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Die tvOS Focus Engine funktioniert am besten mit statischen, vorhersagbaren Fokus-Pfaden. Dynamische Änderungen an <code>destinations</code> während des Renderns können zu Fokus-Verlust oder unerwartetem Verhalten führen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Hoch</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><p><strong>Code-Beispiel:</strong></p><pre><code class="language-typescript">// Neuer Hook für Focus Management
const useFocusGuide = (containerRef: RefObject&lt;View&gt;) =&gt; {
  const [destinations, setDestinations] = useState&lt;any[]&gt;([]);
  
  useEffect(() =&gt; {
    if (!Platform.isTV || !containerRef.current) return;
    
    // Finde alle fokussierbaren Elemente im Container
    const focusableElements = findFocusableElements(containerRef.current);
    setDestinations(focusableElements);
  }, [containerRef]);
  
  return destinations;
};
</code></pre><hr><h4>2.2 tvOS Remote Control Events</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Plattform-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>PlayerScreen.tsx</code> verwendet <code>useTVEventHandler</code> für Remote-Events. Die Behandlung von <code>channelUp</code>/<code>channelDown</code> (Zeile 630-635) und <code>pageUp</code>/<code>pageDown</code> ist korrekt implementiert. Jedoch fehlt die Unterstützung für Long-Press-Events und Gesture-Recognition.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung von Long-Press-Handling für Seek-Operationen (z.B. 10s Skip bei kurzem Druck, 30s bei langem Druck). Hinzufügen von <code>tvParallaxProperties</code> für subtile visuelle Feedback-Effekte auf fokussierte Elemente.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Moderne tvOS-Apps nutzen Long-Press für erweiterte Funktionen. Die Siri Remote (2. Generation und neuer) unterstützt verschiedene Touch-Gesten, die aktuell nicht genutzt werden.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><hr><h4>2.3 iOS Background Audio Support</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Plattform-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>Info.plist</code> enthält keine <code>UIBackgroundModes</code> für Audio. <code>KSPlayerView.swift</code> setzt <code>registerRemoteControll = false</code> (Zeile 169), was Media-Controls im Lock-Screen deaktiviert.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Hinzufügen von <code>audio</code> zu <code>UIBackgroundModes</code> in Info.plist. Implementierung von <code>MPRemoteCommandCenter</code> für Lock-Screen-Controls und <code>MPNowPlayingInfoCenter</code> für Metadaten-Anzeige. Aktivierung von <code>registerRemoteControll = true</code> für KSPlayer.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Benutzer erwarten die Steuerung von IPTV-Streams über das Control Center und Lock-Screen. Dies ist ein Standard-Feature für Audio/Video-Apps auf iOS.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Hoch</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>iOS</td> </tr> </tbody></table><hr><h4>2.4 Picture-in-Picture (PiP) Support</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Plattform-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td>Keine PiP-Implementierung vorhanden. Die App unterstützt nur Vollbild-Wiedergabe.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Für iOS: Verwendung von <code>AVPictureInPictureController</code> mit <code>SwiftTSPlayerView</code>. Für tvOS: Implementierung von <code>UIUserInterfaceIdiom</code>-Check für PiP-Unterstützung (verfügbar ab tvOS 14).</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>PiP ist ein erwartetes Feature für Video-Apps auf iOS/tvOS und ermöglicht Multitasking. Besonders nützlich für IPTV, wo Benutzer nebenbei andere Apps nutzen möchten.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>iOS/tvOS (Beide)</td> </tr> </tbody></table><hr><h3>3. UI/UX-VERBESSERUNGEN</h3><hr><h4>3.1 Accessibility</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>UI/UX</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>ChannelList.tsx</code> hat grundlegende Accessibility-Props (<code>accessibilityRole</code>, <code>accessibilityLabel</code>, <code>accessibilityHint</code>), aber die Implementierung ist inkonsistent. <code>CategoryItem</code> nutzt <code>accessibilityRole="tab"</code>, während <code>ChannelRow</code> <code>accessibilityRole="button"</code> verwendet.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Konsistente Verwendung von <code>accessibilityRole</code> basierend auf Element-Typ. Hinzufügen von <code>accessibilityValue</code> für aktuelle Zustände (z.B. "Kanal 5 von 120"). Implementierung von <code>accessibilityActions</code> für Swipe-Gesten auf Kanälen.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>VoiceOver-Benutzer auf iOS/tvOS benötigen konsistente und informative Accessibility-Hints, um die App effizient zu nutzen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>Beide</td> </tr> </tbody></table><hr><h4>3.2 tvOS Overscan Compensation</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>UI/UX</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>HomeTVLayout.tsx</code> verwendet <code>insets.top</code> und <code>insets.bottom</code> für Safe-Area, aber keine horizontalen Insets für Overscan. Die Sidebar hat <code>width: 300</code> ohne Berücksichtigung von Bildschirmrändern.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Verwendung von <code>useSafeAreaInsets()</code> für alle Seiten. Hinzufügen von horizontalen Margins (`left: insets.left</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Ältere Fernseher und некоторые Projektoren schneiden die Bildränder ab. Apple empfiehlt 40-60px Abstand vom Rand für interaktive Elemente.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><hr><h4>3.3 Loading States und Skeleton Screens</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>UI/UX</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>ChannelList.tsx</code> zeigt nur einen <code>ActivityIndicator</code> beim Laden. <code>PlayerScreen.tsx</code> zeigt "buffering" Text ohne visuelle Progress-Indikation.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung von Skeleton-Screens für Kanallisten mit animierten Platzhaltern. Für Player: Anzeige von Puffer-Progress in Prozent und geschätzter Wartezeit.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Skeleton-Screens werden von Benutzern als schnellerer wahrgenommen als Spinner. Bei IPTV-Streams mit variabler Latenz ist Transparenz über den Ladezustand wichtig.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Niedrig</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>Beide</td> </tr> </tbody></table><hr><h4>3.4 Haptic Feedback (iOS)</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>UI/UX</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td>Keine Haptic-Feedback-Implementierung vorhanden.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Verwendung von <code>Haptics.impactAsync()</code> bei Kanalwechsel, Favorite-Toggle und wichtigen Aktionen. Unterscheidung zwischen <code>light</code>, <code>medium</code> und <code>heavy</code> Impact je nach Aktion.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Haptisches Feedback verbessert die Benutzererfahrung auf iOS-Geräten mit Taptic Engine und gibt Bestätigung für Touch-Interaktionen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Niedrig</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>iOS</td> </tr> </tbody></table><hr><h3>4. CODE-QUALITÄT</h3><hr><h4>4.1 TypeScript Type Safety</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Code-Qualität</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>VideoPlayer.tsx</code> verwendet <code>any</code> für <code>WebVideoComponent</code>, <code>NativeVideoComponent</code>, <code>VLCPlayerComponent</code> und <code>videoRef</code>. <code>PlayerScreen.tsx</code> hat <code>@ts-ignore</code> für <code>TVEventControl</code>.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Definierung von expliziten Type-Interfaces für alle Player-Komponenten. Verwendung von Union-Types statt <code>any</code>. Erstellung einer <code>types/players.ts</code> Datei mit gemeinsamen Typen.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>TypeScript's Hauptvorteil ist Type-Safety. <code>any</code> und <code>@ts-ignore</code> umgehen diese Sicherheit und können zu Runtime-Fehlern führen, die zur Compile-Zeit erkannt werden könnten.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>Beide</td> </tr> </tbody></table><p><strong>Code-Beispiel:</strong></p><pre><code class="language-typescript">// types/players.ts
export interface BasePlayerProps {
  streamUrl: string;
  paused: boolean;
  seekPosition?: number;
  selectedAudioTrackId?: number | null;
  selectedTextTrackId?: number | null;
  onProgress: (data: ProgressData) =&gt; void;
  onVideoLoad: (metadata: VideoMetadata) =&gt; void;
  onPlaybackError: (message?: string) =&gt; void;
}

export interface VLCPlayerProps extends BasePlayerProps {
  initOptions: string[];
  hardwareAcceleration: boolean;
}

export interface KSPlayerProps extends BasePlayerProps {
  hardwareDecode: boolean;
  asynchronousDecompression: boolean;
  displayFrameRate: boolean;
}
</code></pre><hr><h4>4.2 Error Handling Verbesserung</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Code-Qualität</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>SwiftTSPlayerView.swift</code> fängt Errors mit <code>localizedDescription</code> ab, verliert aber wichtige Debug-Informationen. <code>KSPlayerView.swift</code> loggt Errors nur mit <code>print()</code> ohne strukturiertes Error-Tracking.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung eines zentralen Error-Handlers mit Error-Domains und Error-Codes. Verwendung von <code>OSLog</code> für strukturiertes Logging mit verschiedenen Log-Levels. Integration von Crashlytics oder ähnlichen Services für Production-Error-Tracking.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Strukturiertes Error-Handling erleichtert Debugging und ermöglicht bessere User-Feedback-Messages. <code>OSLog</code> ist performanter als <code>print()</code> und erlaubt Filterung in Console.app.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>Beide</td> </tr> </tbody></table><hr><h4>4.3 Swift Memory Management</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Code-Qualität</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>SwiftTSPlayerProxy.swift</code> verwendet <code>activeConnections</code> Array ohne Thread-Safety. Die <code>ConnectionContext</code>-Klasse hält starke Referenzen auf <code>connection</code> und <code>delegate</code>.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Verwendung einer Thread-sicheren Collection für <code>activeConnections</code> mit <code>NSLock</code> oder serialisiertem DispatchQueue. Implementierung von <code>weak</code> Referenzen wo möglich, um Retain Cycles zu vermeiden.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Der Proxy läuft auf einem Background-Queue und erhält Verbindungen auf einem anderen. Ohne Thread-Safety können Race Conditions zu Crashes führen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Hoch</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>iOS/tvOS (Beide)</td> </tr> </tbody></table><hr><h4>4.4 Code Organization</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>Code-Qualität</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>VideoPlayer.tsx</code> (520+ Zeilen) enthält mehrere Player-Render-Funktionen in einer Datei. <code>PlayerScreen.tsx</code> (900+ Zeilen) hat View-Logik und Business-Logik gemischt.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Extraktion von Player-Render-Funktionen in separate Dateien (<code>players/VLCPlayer.tsx</code>, <code>players/KSPlayerWrapper.tsx</code>, etc.). Verwendung von Custom-Hooks für Player-Logik (<code>usePlayerState</code>, <code>useChannelNavigation</code>, <code>useTrackSelection</code>).</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Single Responsibility Principle (SRP) verbessert Wartbarkeit und Testbarkeit. Kleinere Dateien sind einfacher zu verstehen und zu debuggen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Niedrig</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>Beide</td> </tr> </tbody></table><hr><h3>5. TVOS-SPEZIFISCHE ANPASSUNGEN</h3><hr><h4>5.1 Focus Engine Optimierung</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>tvOS-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td>Die Sidebar in <code>HomeTVLayout.tsx</code> hat <code>autoFocus</code> auf <code>TVFocusGuideView</code>, aber der Wechsel zwischen Sidebar und Content-Bereich ist nicht optimal. Bei Kanalwechsel bleibt der Fokus manchmal auf der Sidebar.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung einer <code>useFocusState</code>-Hook, die den aktuellen Fokus-Bereich tracked. Verwendung von <code>nextFocusUp/Down/Left/Right</code> Props für explizite Fokus-Navigation. Bei Kanalwechsel: automatischer Fokus auf den neuen Kanal in der Liste.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>tvOS-Benutzer erwarten deterministische Fokus-Navigation. Unvorhersagbares Verhalten führt zu Frustration.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Hoch</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><hr><h4>5.2 Top Shelf Extension</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>tvOS-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td>Keine Top Shelf Extension implementiert.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung einer Top Shelf Extension mit kürzlich gesehenen Kanälen und Favoriten. Verwendung von <code>TVTopShelfItem</code> für Deep-Links direkt in die Wiedergabe.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Top Shelf ist ein wichtiges tvOS-Feature, das schnellen Zugriff auf Inhalte vom Home-Screen ermöglicht. Es ist ein Standard für Video-Apps auf Apple TV.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Mittel</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><hr><h4>5.3 Siri Remote Gestures</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>tvOS-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td><code>PlayerScreen.tsx</code> behandelt nur Button-Events (<code>select</code>, <code>menu</code>, <code>playPause</code>, etc.). Touch-Gesten auf dem Touchpad der Siri Remote werden nicht genutzt.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Implementierung von Swipe-Gesten für Kanalwechsel (Swipe Up/Down) und Lautstärke-Steuerung (Swipe Left/Right). Verwendung von <code>PanResponder</code> für feingranulare Touch-Steuerung.</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>Die Siri Remote bietet Touch-Interaktionen, die intuitive Navigation ermöglichen. Swipe-Gesten sind natürlicher für Kanalwechsel als Button-Drucke.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Niedrig</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><hr><h4>5.4 tvOS Settings Bundle</h4><table class="e-rte-table"> <thead> <tr> <th>Attribut</th> <th>Wert</th> </tr> </thead> <tbody><tr> <td><strong>Kategorie</strong></td> <td>tvOS-spezifisch</td> </tr> <tr> <td><strong>Aktueller Zustand</strong></td> <td>App-spezifische Settings sind nur innerhalb der App verfügbar, nicht in tvOS Settings App.</td> </tr> <tr> <td><strong>Vorgeschlagene Änderung</strong></td> <td>Erstellung eines Settings Bundle mit <code>Settings.bundle</code> für grundlegende Optionen (Standard-Player, Hardware-Decoding, Sprache).</td> </tr> <tr> <td><strong>Begründung</strong></td> <td>tvOS-Benutzer suchen oft Einstellungen in der System-Settings-App. Ein Settings Bundle verbessert die Auffindbarkeit wichtiger Optionen.</td> </tr> <tr> <td><strong>Priorität</strong></td> <td>Niedrig</td> </tr> <tr> <td><strong>Plattform</strong></td> <td>tvOS</td> </tr> </tbody></table><hr><h2>Priorisierte Zusammenfassung</h2><h3>Hohe Priorität (Sofort angehen)</h3><ol> <li><strong>Speicherverwaltung in Native Playern</strong> - Memory Leaks verhindern</li> <li><strong>iOS Background Audio Support</strong> - Standard-Feature für Video-Apps</li> <li><strong>tvOS Focus Engine Optimierung</strong> - Kern-UX für Apple TV</li> <li><strong>Swift Thread Safety</strong> - Race Conditions vermeiden</li> </ol><h3>Mittlere Priorität (Nächste Iteration)</h3><ol start="5"> <li><strong>FlatList Virtualization Tuning</strong> - Performance bei großen Listen</li> <li><strong>tvOS Overscan Compensation</strong> - Kompatibilität mit älteren TVs</li> <li><strong>TypeScript Type Safety</strong> - Code-Qualität verbessern</li> <li><strong>Error Handling Verbesserung</strong> - Besseres Debugging</li> <li><strong>tvOS Top Shelf Extension</strong> - Wichtiges tvOS-Feature</li> <li><strong>Bild-Caching Optimierung</strong> - Netzwerk-Performance</li> </ol><h3>Niedrige Priorität (Langfristig)</h3><ol start="11"> <li><strong>Code Organization</strong> - Refactoring für Wartbarkeit</li> <li><strong>Skeleton Screens</strong> - UX-Polish</li> <li><strong>Haptic Feedback</strong> - iOS UX-Verbesserung</li> <li><strong>Siri Remote Gestures</strong> - Erweiterte tvOS-Interaktion</li> <li><strong>tvOS Settings Bundle</strong> - Benutzerfreundlichkeit</li> </ol><hr><h2>Architektur-Empfehlungen</h2><h3>Empfohlene Ordnerstruktur für Native Module</h3><pre><code>modules/
├── react-native-ksplayer/
│   ├── ios/
│   │   ├── KSPlayerView.swift
│   │   ├── KSPlayerViewManager.swift
│   │   ├── Extensions/
│   │   │   ├── KSPlayer+ErrorHandling.swift
│   │   │   └── KSPlayer+RemoteControl.swift
│   │   └── Utils/
│   │       ├── Logger.swift
│   │       └── StreamDetector.swift
│   └── ...
├── react-native-swift-ts-player/
│   ├── ios/
│   │   ├── SwiftTSPlayerView.swift
│   │   ├── SwiftTSPlayerProxy.swift
│   │   └── PiP/
│   │       └── PictureInPictureController.swift
│   └── ...
</code></pre><h3>Empfohlene React Native Struktur</h3><pre><code>components/
├── player/
│   ├── VideoPlayer.tsx (Hauptkomponente)
│   ├── players/
│   │   ├── VLCPlayer.tsx
│   │   ├── KSPlayerWrapper.tsx
│   │   └── AVPlayerView.tsx
│   └── hooks/
│       ├── usePlayerState.ts
│       ├── useTrackSelection.ts
│       └── usePlayerError.ts
├── channels/
│   ├── ChannelList.tsx
│   ├── ChannelRow.tsx (memoized)
│   ├── CategoryItem.tsx (memoized)
│   └── hooks/
│       ├── useChannelNavigation.ts
│       └── useChannelEpg.ts
└── tv/
    ├── TVFocusGuideView.tsx
    ├── TVSidebar.tsx
    └── hooks/
        ├── useFocusGuide.ts
        └── useTVRemote.ts
</code></pre><hr><h2>Fazit</h2><p>CouchPotatoPlayer zeigt eine solide Architektur mit professionellem Design. Die wichtigsten Verbesserungen betreffen die Speicherverwaltung in den nativen Player-Modulen und die tvOS-spezifische Focus-Engine-Integration. Die React-Native-Codebasis ist bereits gut optimiert mit <code>React.memo</code> und <code>useCallback</code>, kann aber durch weitere Modularisierung und Custom-Hooks verbessert werden.</p><p>Die Implementierung der empfohlenen Änderungen wird die Benutzererfahrung signifikant verbessern, insbesondere auf Apple TV, wo die Focus-Navigation und Overscan-Kompensation kritisch für eine gute UX sind.</p>