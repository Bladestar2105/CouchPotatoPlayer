Look at this compiler flag for `media_kit_libs_ios_video`:
`-parse-as-library`

What about `path_provider_foundation` module compilation?
```
/Applications/Xcode_16.4.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/swift-frontend -frontend -c /Users/runner/.pub-cache/hosted/pub.dev/path_provider_foundation-2.5.1/darwin/path_provider_foundation/Sources/path_provider_foundation/messages.g.swift ... -I /Users/runner/work/CouchPotatoPlayer/CouchPotatoPlayer/build/ios/Release-appletvos/path_provider_foundation -F /Users/runner/work/CouchPotatoPlayer/CouchPotatoPlayer/build/ios/Release-appletvos/path_provider_foundation -F /Users/runner/work/CouchPotatoPlayer/CouchPotatoPlayer/ios/Flutter ...
```
Is there a difference in how Swift finds modules between `-I` and `-F`? No.
Wait! What if the `Flutter` framework is symlinked inside `ios/Flutter`, and CocoaPods requires the REAL path? No, we used `File.expand_path`.
What if `Flutter.framework` is NOT A MODULE?!
Wait... what if `url_launcher_ios` ACTUALLY DID NOT BUILD!
Let's check the logs very closely.
```
SwiftCompile normal arm64 Compiling\ Launcher.swift,\ messages.g.swift,\ URLLauncherPlugin.swift,\ URLLaunchSession.swift /Users/runner/.pub-cache/hosted/pub.dev/url_launcher_ios-6.3.6/ios/url_launcher_ios/Sources/url_launcher_ios/Launcher.swift ... (in target 'url_launcher_ios' from project 'Pods')
CompileSwift normal arm64 (in target 'url_launcher_ios' from project 'Pods')
```
If it prints `CompileSwift normal arm64`, it means the Swift compiler command *finished*. But wait! Did it finish with SUCCESS?
YES! If it had failed, Xcode would print `Command SwiftCompile failed with a nonzero exit code` for `url_launcher_ios` AND it would print the `Failed frontend command:` with the exact arguments!
BUT IT DIDN'T DO THAT for `url_launcher_ios`! It ONLY did it for `path_provider_foundation` and `media_kit_video`!
Therefore, `url_launcher_ios` succeeded.
If `url_launcher_ios` succeeded, `Flutter` module EXISTS AND WORKS.

Wait. If `Flutter` module exists and works, could `path_provider_foundation` be looking for a DIFFERENT `Flutter` module?
What if `path_provider_foundation.podspec` has something like this:
```
s.ios.dependency 'Flutter'
s.osx.dependency 'FlutterMacOS'
```
Wait! I patched `messages.g.swift` to:
```swift
#if os(iOS) || os(tvOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#endif
```
Is it possible that `path_provider_foundation` needs `FlutterMacOS` instead, because of some other setting? No, `os(tvOS)` triggers `import Flutter`.
Wait... look at `path_provider_foundation` again!
`error: no such module 'Flutter'`
Wait, if it complains about `Flutter`, it tried to load `Flutter`.
Wait! Is it possible `path_provider_foundation` is missing the `modulemap` search path?
In `url_launcher_ios`, the `podspec` has:
```
s.pod_target_xcconfig = { 'DEFINES_MODULE' => 'YES' }
```
In `path_provider_foundation`, it DOES NOT!
If `DEFINES_MODULE` is NOT YES, does `path_provider_foundation` fail to find modules? No, `DEFINES_MODULE` affects whether `path_provider_foundation` ITSELF is a module.
What about `s.ios.xcconfig` vs `s.xcconfig`?!
Look at `url_launcher_ios`:
```
  s.xcconfig = {
      'LIBRARY_SEARCH_PATHS' => '$(TOOLCHAIN_DIR)/usr/lib/swift/$(PLATFORM_NAME)/ $(SDKROOT)/usr/lib/swift',
      'LD_RUNPATH_SEARCH_PATHS' => '/usr/lib/swift',
  }
```
Look at `path_provider_foundation`:
```
  s.ios.xcconfig = {
    'LIBRARY_SEARCH_PATHS' => '$(TOOLCHAIN_DIR)/usr/lib/swift/$(PLATFORM_NAME)/ $(SDKROOT)/usr/lib/swift',
    'LD_RUNPATH_SEARCH_PATHS' => '/usr/lib/swift',
  }
```
If we build for tvOS, `s.ios.xcconfig` IS IGNORED BY COCOAPODS AND XCODE!
Why? Because CocoaPods might wrap it in `[sdk=iphoneos*]`!
Wait! Let's check the `.xcconfig` generated by CocoaPods for `path_provider_foundation` in `test_dl`!
Actually, I can't check it without running `pod install`.
BUT if `s.ios.xcconfig` is ignored, `LIBRARY_SEARCH_PATHS` and `LD_RUNPATH_SEARCH_PATHS` are MISSING!
Could missing `LIBRARY_SEARCH_PATHS` or `LD_RUNPATH_SEARCH_PATHS` cause `no such module 'Flutter'`?
No! `Flutter` is a framework, searched via `-F`. `LIBRARY_SEARCH_PATHS` is for static/dynamic libraries (`.a`, `.dylib`).
Wait... does the Swift compiler need `LIBRARY_SEARCH_PATHS` to find standard libraries?
The `sdk` flag gives standard libraries.

Wait... if CocoaPods wraps `FRAMEWORK_SEARCH_PATHS` in `[sdk=iphoneos*]`, DOES IT ALSO DO THAT FOR DEPENDENCIES?!
YES! CocoaPods writes:
`FRAMEWORK_SEARCH_PATHS = $(inherited) ...`
BUT for platform-specific dependencies:
`FRAMEWORK_SEARCH_PATHS[sdk=iphoneos*] = $(inherited) .../Flutter`
Wait, does it do that?
Yes! Since CocoaPods 1.10, multi-platform podspecs generate platform-specific conditional build settings!
If CocoaPods generates `FRAMEWORK_SEARCH_PATHS[sdk=iphoneos*] = .../Flutter`, then when Xcode builds for `appletvos`, it DOES NOT SEE `.../Flutter` from CocoaPods!
BUT WAIT! I ALREADY ESTABLISHED THAT `tvos/Podfile` ADDS `-F .../ios/Flutter` MANUALLY!
`config.build_settings['FRAMEWORK_SEARCH_PATHS'] = '"' + flutter_dir + '" $(inherited)'`
This ADDS `-F .../ios/Flutter` directly to the `pbxproj`!
And the `xcodebuild` log shows:
`-F /Users/runner/work/CouchPotatoPlayer/CouchPotatoPlayer/ios/Flutter`
So the path IS passed to `swift-frontend`!
Why would `swift-frontend` say `no such module 'Flutter'` if the path IS passed?!
