events {}
http {
    error_log /app/error.log debug;
    server {
        listen 80;

        # In test28, `curl -I "http://localhost:80/proxy/https://stream.team-ei.de/"` returned 200 OK!
        # Which matched the REGULAR /proxy/ block.
        # But wait, what if I curl `http://localhost:80/proxy/https://stream.team-ei.de/test` (NOT mp4)?
        # In test28, it returned 404 Not Found from CADDY!
        # Which means it works perfectly!
        # SO, why does the REGEX block fail?
        # Let's see what proxy_pass does in the REGULAR /proxy/ block for `https://stream.team-ei.de/test`
    }
}
