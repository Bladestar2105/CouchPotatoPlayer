## Instructions
[unknown] - When updating profiles within context/IPTVContext.tsx context loaders (e.g., attaching API metadata), avoid replacing the entire profile object using generic setter functions if doing so triggers cascading useEffect dependencies. Use functional state updates (setProfiles(prev => ...)) inside targeted utility functions to safely merge data and prevent infinite render loops.
[unknown] - To optimize high-volume data parsing and cache hydration (e.g., EPG data), use primitive numeric timestamps (number) instead of instantiating Date objects within large loops. This significantly reduces CPU overhead and memory allocations from expensive object creation.
[unknown] - To suppress noisy native diagnostic logs from the KSPlayer/FFmpegKit engine (e.g., missing codec parsers or POC errors), set KSLog.logLevel = .off directly in the react-native-ksplayer Swift module (KSPlayerView.swift).
[unknown] - Do not commit unintentional lockfile modifications (such as pnpm-lock.yaml or bun.lock changes introduced by local test dependency installations) in unrelated PRs to avoid dependency conflicts and unintended removal of core packages (e.g., react-native-vector-icons). Always revert these lockfile changes before committing.
[unknown] - When testing conditional logic dependent on the React Native global __DEV__ flag using bun:test, mock it by casting and reassigning (globalThis as any).__DEV__. Always save the original value before tests and restore it in an afterEach hook to prevent state leakage and test bleed.
[unknown] - For performance optimization tasks under the 'Bolt' persona, PR titles must be formatted exactly as '⚡ Bolt: [performance improvement]' and descriptions must include 'What', 'Why', 'Impact', and 'Measurement' sections. Critical learnings discovered during optimization should be appended to .jules/bolt.md using the 'YYYY-MM-DD - [Title]' format.
[unknown] - When updating log or journal files such as .Jules/palette.md or .jules/bolt.md, always append new entries instead of overwriting the file to prevent the destruction of past historical data.
[unknown] - To prevent React Native event registration warnings ('re-registered direct event as a bubbling event') when integrating multiple native UI components that might share event names (e.g., react-native-video, KSPlayerView, SwiftTSPlayerView), namespace the native event props in the Objective-C/Swift view managers (e.g., onKSVideoLoad instead of onVideoLoad).
[unknown] - To maintain focus on the first item of a dynamically rendered list in tvOS (e.g., when changing categories), temporarily set hasTVPreferredFocus={true} on the target item (index === 0) and quickly reset the trigger state via an effect setTimeout to safely hand control back to the native tvOS focus engine without lingering states.
[unknown] - When parsing Xtream API data for Catchup/Archive channels, account for both integer and string data variations for archive availability (e.g., tv_archive === 1 or '1') and duration properties (tv_archive_duration or tvArchiveDuration).
[unknown] - For security tasks under the 'Sentinel' persona, PR titles must be formatted as '🛡️ Sentinel: [Severity] Fix [vulnerability]' or '🛡️ Sentinel: [security improvement]'. Fixes must be kept under 50 lines. Critical security learnings must be appended to .jules/sentinel.md using the exact format: '## YYYY-MM-DD - [Title]', followed by '**Vulnerability:**', '**Learning:**', and '**Prevention:**' sections.
[unknown] - To prevent URL injection and path traversal vulnerabilities, and to ensure credentials containing special characters (like /, ?, or #) are safely transmitted, always apply encodeURIComponent to parameters such as username and password when interpolating them into API endpoint URLs (e.g., Xtream Codes timeshift/catchup URLs).
[unknown] - In React Native routing (especially tvOS), ensure navigation.goBack() is wrapped in if (navigation.canGoBack()) conditionals with a fallback layout (e.g., navigation.navigate('Home')) to prevent fatal app crashes when navigating back from an empty stack.
[unknown] - The project explicitly does not support Expo Go due to its reliance on native video player libraries (e.g., react-native-vlc-media-player, react-native-video). All local development and testing must use a Custom Development Client (Prebuild) for the target device or simulator.
[unknown] - For micro-UX enhancements under the 'Palette' persona, PR titles must be exactly '🎨 Palette: [UX improvement]' and descriptions must include 'What', 'Why', 'Before/After', and 'Accessibility' sections. Changes must be under 50 lines, strictly use existing CSS/classes (no custom CSS), guarantee keyboard accessibility (ARIA labels, focus states), exclusively use pnpm, and document critical new UX/accessibility learnings by appending to .Jules/palette.md using the '## YYYY-MM-DD - [Title]' format.
[unknown] - When replacing insecure ID generation (e.g., Math.random().toString() or Date.now().toString()), use crypto.randomUUID() by default. To ensure cross-environment compatibility (like React Native), implement a defensive fallback: (typeof crypto !== 'undefined' && crypto.randomUUID) ? crypto.randomUUID() : Date.now().toString(). However, in performance-critical loops with massive arrays (e.g., EPG data parsing in context/IPTVContext.tsx), avoid cryptographic functions and construct deterministic IDs using string concatenation (e.g., ${channelId}_${start}) to prevent blocking the JS thread.
[unknown] - Do not plan or attempt any migration to Flutter for this project. The user has explicitly directed that a migration to Flutter will not be done, and any mentions of it must be removed.
[unknown] - When using the set_plan tool to create an execution plan, the pre-commit step description must strictly be phrased exactly as: 'Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.' to successfully pass the request_plan_review checks.
[unknown] - In the custom react-native-ksplayer native module for iOS/tvOS, advanced playback features like hardware decoding, asynchronous decompression, and adaptive frame rate (implemented natively via KSOptions setting display = .plane and clearing video_size) are exposed and toggled via React Native props: hardwareAcceleration, asyncDecompression, and adaptiveFrameRate.
[unknown] - In React Native, fetch aggressively caches responses. To force fresh network requests and bypass cache (e.g., for playlist/EPG updates), include Cache-Control: no-cache and Pragma: no-cache headers in the fetch options.
[unknown] - In React Native TV applications, sidebar menus relying on onFocus and onBlur events can flicker or bounce during rapid navigation due to event timing overlaps. To prevent this, use a useRef counter to track active focus state across items, only collapsing the menu when the focus count reaches zero.
[unknown] - Untyped native and third-party modules such as react-native-swift-ts-player, react-native-ksplayer, and lucide-react-native require explicit declare module definitions in a react-native.d.ts file to prevent tsc compilation errors.
[unknown] - React Native JS warnings (like 'InteractionManager has been deprecated' and 'Persistent storage is not supported on tvOS') are intentionally suppressed in utils/logger.ts using LogBox.ignoreLogs. Note that this only hides the yellow box UI; it does not stop Metro from logging them in the terminal, nor can it suppress native iOS/tvOS logs (like 'UIKitCore' or 'ImageIO').
[unknown] - To make React Native <Switch> components interactive on tvOS, wrap their container in a <TouchableOpacity disabled={!Platform.isTV}> and trigger the state change via onPress. Apply pointerEvents={Platform.isTV ? 'none' : 'auto'} to the <Switch> to prevent focus interception on Apple TV while preserving native drag/tap behavior on iOS and Android.
[unknown] - For responsive UI during heavy video player unmounting or screen transitions, immediately hide the heavy component via state (e.g., isExiting = true) and defer the synchronous teardown/navigation logic to the next tick using setTimeout.
[unknown] - In React Native, conditionally rendering components using logical AND (&&) with numeric properties (e.g., value && <Component/>) causes crashes with the error 'Text strings must be rendered within a <Text> component' if the value is 0. Always ensure the condition evaluates strictly to a boolean (e.g., !!value) or use a ternary operator (value ? <Component/> : null) to prevent numeric 0 from being rendered directly.
[unknown] - To prevent Log Injection (CRLF) vulnerabilities in Node.js servers, untrusted inputs such as requested URLs or network error messages must be sanitized before being passed to console.log or console.error. Explicitly strip carriage return (\r) and line feed (\n) characters (e.g., msg.replace(/[\r\n]/g, '')) to prevent attackers from forging log entries.
[unknown] - When running bun test on files that import React Native or Expo dependencies, a setup-tests.ts file must be created and preloaded via a bunfig.toml configuration or CLI flag (--preload ./setup-tests.ts) to mock native globals like LogBox, Platform, __DEV__, TurboModuleRegistry, NativeEventEmitter, and globalThis.expo to prevent reference errors during evaluation.
[unknown] - Native React Native Button components do not fully support accessibilityLabel or nested custom styling. For improved accessibility and consistent cross-platform styling, default to using TouchableOpacity or Pressable with explicit accessibilityRole="button", accessibilityLabel, and accessibilityHint attributes instead. This is especially critical for primary call-to-action buttons containing only stylized text or icons to ensure screen readers provide full context.
[unknown] - In Python proxy servers utilizing http.server.BaseHTTPRequestHandler, the default log_message implementation writes the full raw request line to sys.stderr, risking information exposure of credentials in the URL. Override log_message to sanitize output: safely decode the message (urllib.parse.unquote), mask Basic Auth and query parameter credentials using regex, and re-encode (urllib.parse.quote) before logging to prevent both credential leaks and log injection (CRLF) vulnerabilities.
[unknown] - When dealing with type inconsistencies from external API data (e.g., Xtream API numbers represented as strings), avoid using hacky TypeScript type assertions (like val === '1' as unknown as number) in production code. Use proper runtime type coercion (e.g., String(val) === '1' or Number(val) > 0). In test files, casting mock properties using as any is acceptable to simulate these edge cases without breaking strict interface definitions.
[unknown] - Avoid patching react-native core files (via pnpm patch) just to silence internal JS deprecation warnings. If LogBox.ignoreLogs is insufficient for the terminal output, tolerate the warnings rather than maintaining fragile core patches.
[unknown] - To prevent garbage collection spikes and main thread blocking in hot render paths (like virtualized lists or EPG timelines), avoid instantiating Date objects on every render. Instead, pass primitive numeric timestamps directly to Intl.DateTimeFormat.format(), which natively accepts them.
[unknown] - In React Native TV apps, to restore focus to a specific item in a list when returning from another screen (e.g., navigating back from a player), pass the item's ID via navigation route params (e.g., focusChannelId) and use a useEffect on the list screen to evaluate route.params and update the local focused state.
[unknown] - To support direct playback of .ts IPTV streams natively on iOS/tvOS, SwiftTSPlayerProxy in the react-native-swift-ts-player module tunnels streams through a local dynamic .m3u8 playlist. To prevent AVPlayer from instantly freezing, native formats (like .m3u8 or .mp4) must completely bypass this proxy. Additionally, the proxy must properly parse and forward client HTTP Range requests and accurately return server response codes (e.g., 206 Partial Content) and headers (stripping Transfer-Encoding) to support AVPlayer buffering.
[unknown] - In performance-critical React Native contexts (like large FlatList items, EPG timelines, or category/group selections), avoid using O(N) array methods (e.g., Array.some(), Array.find()) for state lookups or inside useEffect dependencies. Instead, derive an O(1) Set or dictionary Map using useMemo to prevent main thread blocking.
[unknown] - For testing improvement tasks, PR titles must be formatted exactly as '🧪 [testing improvement description]' and descriptions must include '🎯 What', '📊 Coverage', and '✨ Result' sections.
[unknown] - When iterating over massive arrays (e.g., tens of thousands of channels or VOD items) where early returns are expected, use traditional for loops instead of high-order functions like Array.prototype.some(), Array.prototype.every(), or Array.prototype.find(). This avoids garbage collection overhead from closure instantiation and significantly improves execution speed by preventing main thread blocking.
[unknown] - To make React Native TouchableOpacity buttons (such as Info or Control icons) focusable and selectable with an Apple TV or Android TV remote, explicitly provide TV focus props: accessible={true}, isTVSelectable={true}, hasTVPreferredFocus={false}, and tvParallaxProperties={{ enabled: false }}.
[unknown] - When running the local Expo web development server (npx expo start --web), the application may fail to render and log requireNativeComponent is not a function errors. This is due to custom native media player modules lacking web implementations. The web environment can still be partially used to verify pre-render syntax or layout, but expect failures on native-dependent screens.
[unknown] - To optimize high-volume data parsing (such as M3U playlists in context/IPTVContext.tsx), utilize nested Map structures to replace O(N) linear array searches with O(1) lookups and defer all sorting operations until after the primary parsing loop is complete to minimize computational overhead.
[unknown] - On React Native tvOS, to ensure elements outside the linear focus path (like absolute positioned top-bar back or info buttons) are wrapping them in a TVFocusGuideView and pass their refs to the destinations prop. Ensure the destinations array only contains active, mounted refs to avoid stale reference focus issues.
[2025-05-23] - Optimize EPG Timeline Rendering with Binary Search: In a UI layout engine rendering horizontally scrollable timelines (like an EPG grid), iterating over the entire program array (length > 10,000 with multiple days of data) for every single channel row using a simple O(N) for loop causes extreme UI blockages. Action: Since timeline program data is inherently chronologically sorted, use an O(log N) binary search to find the index of the first program that falls within the visible timeline window. Start the render loop from this index and immediately break when a program's start time exceeds the visible window.

## Identity
[unknown] - Name: Stelios Sakalis. Email: stelios@sakalis.eu. Platform Username: bladestar2105.

## Career
[unknown] - No career information has been stored in memory yet.

## Projects
[unknown] - IPTV / React Native App: A cross-platform application for TV and Mobile. It exclusively supports Xtream Codes and M3U playlists (Stalker portal support was explicitly removed). It uses @react-navigation/native for routing and React Context (IPTVContext, SettingsContext) for state management. Tests are written with bun:test and co-located in __tests__ folders. The app supports interchangeable native media player engines including a custom react-native-swift-ts-player (for iOS/tvOS Apple native playback, handling .ts streams via a local SwiftTSPlayerProxy .m3u8 tunnel), react-native-video, and react-native-vlc-media-player. Expo Go and expo-av are explicitly incompatible and unsupported.

## Preferences
[unknown] - State management in the application is primarily handled via React Context (e.g., IPTVContext, SettingsContext), not external libraries like Zustand or Redux.
[unknown] - The package.json file does not currently define generic build, lint, or test scripts. Running tsc or jest natively often fails due to missing global installs or environment-specific configuration issues (e.g., ESM SyntaxErrors in tests). Execute tests using bun test instead.
[unknown] - The repository is structured with root-level source directories (e.g., screens/, components/, utils/, context/) rather than an encapsulating src/ folder.
[unknown] - Unit tests are co-located with their source files inside a __tests__ directory (e.g., utils/__tests__/catchupUtils.test.ts) and utilize the bun:test framework.
