events {}
http {
    error_log /app/error.log debug;
    server {
        listen 80;

        # AHAHA! The regular /proxy/ block STILL sends "/proxy/https:/stream.team-ei.de/test?" !!
        # Which means both blocks send the WRONG URI!
        # And BOTH blocks fail!
        # But wait! If they both send the wrong URI, why did /proxy/https://stream.team-ei.de/ return 200 OK?
        # Because Caddy on that domain responds to GET "/proxy/https:/stream.team-ei.de/" with 200 OK?
        # YES! Caddy serves a default page, so ANY route it doesn't recognize returns the default page or 404!
        # So we HAVE to fix proxy_pass in BOTH blocks to NOT send `/proxy/https:/...`!

        # But wait, Nginx memory says:
        # "In nginx.conf, the sub_filter directive does not accept an 'off' argument and requires exactly two arguments. To prevent binary video stream corruption (e.g., .ts, .mp4) without causing syntax errors, rely on Nginx's lack of inheritance between sibling location blocks rather than explicitly disabling it. Isolating media segments into their own regex location block ensures sub_filter from the sibling /proxy/ block is naturally not applied."
        # AND:
        # "Both frontend API requests and direct media stream playback URLs (e.g., in getLiveStreamUrl) must append the target URL directly to the proxy path (e.g., /proxy/${url}) instead of using a URL-encoded query parameter, because Nginx extracts the raw target URL directly from $request_uri to avoid proxy_pass encoding errors."

        # The issue the user reported is 502 Bad Gateway for .mp4 URLs!
        # The user has explicitly set up a regex block to disable sub_filter!
        # But wait, if both blocks are broken because of `proxy_pass $target_url`, let's check Nginx memory again.

        # How to fix proxy_pass so it sends the CORRECT URI?
        # Nginx documentation: "When variables are used in proxy_pass... if URI is specified in the directive, it is passed to the server as is, replacing the original request URI."
        # Wait, if `proxy_pass $target_url` sends the WRONG URI because `$target_url` contains `https://domain/path`, why does Nginx not recognize the URI?
        # Because we didn't extract the domain and path!

        # BUT WAIT, if we just do:
        # proxy_pass $target_url;
        # Maybe the problem is `proxy_pass` needs to be `proxy_pass $target_url;` where `$target_url` is just a variable but we should clear $request_uri ?

        # No, wait! There is NO WAY to make `proxy_pass $variable` strip the request URI unless the variable contains a URI!
        # BUT `$target_url` IS literally `https://stream.team-ei.de/movie/yoshisan/Ss21051988/1.mp4`! It DOES contain a URI!
        # Why is Nginx ignoring the URI inside the variable?
    }
}
